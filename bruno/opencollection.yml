opencollection: 1.0.0

info:
  name: Express Primer

request:
  auth:
    type: bearer
    token: '{{auth-token}}'

extensions:
  bruno:
    defaultEnvironment: Local
    ignore:
      - node_modules
      - .git
